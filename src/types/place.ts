export interface NaverPlace {
  title: string;
  address: string;
  category: string;
  keyword?: string;
  roadAddress?: string;
  link?: string;
  telephone?: string;
}

export interface PlaceInfo {
  name: string;
  address: string;
  category: string;
}
