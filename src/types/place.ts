export interface NaverPlace {
  title: string;
  address: string;
  category: string;
  roadAddress?: string;
  link?: string;
}

export interface PlaceInfo {
  name: string;
  address: string;
  category: string;
}

